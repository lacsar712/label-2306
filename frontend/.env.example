# Backend API base URL (optional, default: /api)
# When running locally with "npm run dev", Vite dev server will proxy /api to http://localhost:8000
# so you can leave this blank or set it to "/api" for local development.
# For production builds, set this to your actual backend API URL (e.g., "https://api.example.com").
VITE_API_URL=/api
